const nome = "Junior";
const sobrenome = "Rodrigues";
const idade = 20;
const enredeco = "Rua das Flores";

function greetings(name: string) {
	console.log(`Olá ${name}`);
}
greetings("Lucas");

const obj = {
	chave: 2,
	nome: "lucas",
	prop: 10,
};
